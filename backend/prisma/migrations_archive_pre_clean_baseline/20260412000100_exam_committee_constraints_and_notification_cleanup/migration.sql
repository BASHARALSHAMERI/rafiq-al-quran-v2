-- Reconstructed placeholder for a migration that already exists in _prisma_migrations
-- in deployed databases but was missing from the repository history.
-- Intentionally left as a no-op so the local migration chain can be replayed consistently.
SELECT 1;

